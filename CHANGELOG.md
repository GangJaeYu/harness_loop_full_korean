# 변경 기록

[← README](README.md)

## 1.10.0

실사용 프로젝트의 세션 기록을 집계해 나온 비용·정지 문제를 고쳤습니다.

- **코디네이터를 상주시키지 않습니다** (`loop-setup`·`loop-update`). Orca 모드에서 대화형 코디네이터가 phase 내내 살아 있던 구조를 없앴습니다. Orca 터미널 안의 구동 스크립트가 워커를 띄우고 기다리고, 판단은 라운드마다 새로 여는 코디네이터 세션이 합니다. 워커의 질문은 별도 세션이 문서를 보고 답하거나 사람에게 올립니다
- **트리거를 실행 단계로 넣었습니다.** 여러 태스크에 걸친 같은 실패를 코디네이터가 매 라운드 세고(`LOG.md` 의 `- 실패:` 줄), `알림:` 에 처리 주체와 기한을 붙이고, phase 를 열 때 기한이 지난 알림과 판단 시점이 온 미결을 묻습니다
- 이전 버전의 `LOOP.md` 는 `loop-update` 가 새 구조로 옮깁니다
- 테스트가 DB 를 쓰면 슬롯별 DB 격리를 추천합니다
- 워커 워크트리 청소 명령을 `git reset --hard coordinator` 로 고쳤습니다(이전 명령은 다른 워크트리에 체크아웃된 브랜치라 git 이 거부했습니다)
- `harness-setup`·`harness-update` — e2e 흔들림 규칙을 출처가 붙은 표로 두고 흔들림을 고칠 때마다 한 줄씩 늘립니다. `HARNESS.md` 에 공유 DB 를 쓰는 게이트와 슬롯 변수를 적습니다

## 1.9.0

프로젝트 완료 뒤를 다루는 스킬 넷을 더했습니다. 1.6.0~1.8.0 은 개발 저장소에서만 거친 중간 단계라 이 저장소에는 따로 없습니다.

- `code-to-docs` — 문서 없는 코드베이스를 현재 동작 기준 문서·테스트 phase·하네스·루프로 올립니다
- `refactor-plan` — 범위 셋, 고칠 목록, `## 리팩토링 N` 후미 절, 리팩토링 phase
- `feature-add` — 앞 스킬을 후미 절 모드로 불러 `## 추가 N` 절과 기능 phase
- `bug-fix` — 버그/요구 변경 판정, 재현 기록 먼저, 즉시 활성 회귀 검사, 게이트 통과 뒤 커밋 1회
- `plan-generator` — 현재 동작 문서의 `구현됨 (as-is)` 행은 담당 태스크가 붙어도 상태를 올리지 않습니다

## 1.5.3 이하

태그와 커밋 메시지를 보세요.
